type: remap
inputs:
  - kubernetes_host_metrics
source: |
  .tags."@vector.stream" = "metrics.kubernetes.host"
  .tags.host = get_env_var!("VECTOR_SELF_NODE_NAME")
  .tags."kubernetes.cluster" = get_env_var!("KUBERNETES_CLUSTER_NAME")
