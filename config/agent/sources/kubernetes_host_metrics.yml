type: host_metrics
collectors:
  - cpu
  - memory
  - disk
  - filesystem
  - load
  - network
  - process
namespace: host
scrape_interval_secs: 30
filesystem:
  devices:
    excludes: ["binfmt_misc"]
