apiVersion: v1
kind: ServiceAccount
metadata:
  name: vector-secret-store
  namespace: vector
---
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: vector-secret-store
  namespace: vector
spec:
  retrySettings:
    maxRetries: 5
    retryInterval: "10s"
  provider:
    vault:
      server: https://vault.ilysium.io
      path: secrets
      caProvider:
        type: ConfigMap
        name: ilysium-root-ca
        key: ca.crt
      auth:
        kubernetes:
          mountPath: kubernetes
          role: vector
          serviceAccountRef:
            name: vector-secret-store
